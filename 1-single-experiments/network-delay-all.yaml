apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-delay
  namespace: demo
spec:
  action: delay
  mode: all
  selector:
    labelSelectors:
      app.kubernetes.io/instance: sqlserver-ag-cluster
  delay:
    latency: "200ms"
    correlation: "100"