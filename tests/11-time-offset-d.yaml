apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: mssql-time-offset-small
  namespace: chaos-mesh
spec:
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  timeOffset: "-30m"
  clockIds:
    - CLOCK_REALTIME
  duration: "5m"
