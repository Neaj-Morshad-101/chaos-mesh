apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: mssql-node-reboot-short
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
  gracePeriod: 0
  duration: "15s"
