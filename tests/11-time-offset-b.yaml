apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: mssql-time-offset-forward
  namespace: chaos-mesh
spec:
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  timeOffset: "+2h"
  clockIds:
    - CLOCK_REALTIME
  duration: "5m"
