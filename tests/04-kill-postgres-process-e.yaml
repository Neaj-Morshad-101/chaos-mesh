apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: mssql-kill-process-fixed-percent
  namespace: chaos-mesh
spec:
  action: container-kill
  mode: fixed-percent
  value: "66"
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
  containerNames:
    - postgres
  duration: "30s"
