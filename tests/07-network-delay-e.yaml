apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: mssql-delay-one-direction
  namespace: chaos-mesh
spec:
  action: delay
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  target:
    mode: all
    selector:
      namespaces:
        - demo
      labelSelectors:
        "app.kubernetes.io/instance": "sqlserver-ag-cluster"
  delay:
    latency: "500ms"
    jitter: "100ms"
    correlation: "50"
  direction: to
  duration: "5m"
