# go in not ready state, back to ready state once chaos is stopped
apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata:
  name: mssql-io-latency-replica
  namespace: chaos-mesh
spec:
  action: latency
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "secondary"
  volumePath: /var/pv
  path: /var/pv/data/**/*
  delay: "500ms"
  percent: 100
  duration: "5m"
  containerNames:
    - postgres
