apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: mssql-dns-random
  namespace: chaos-mesh
spec:
  action: random
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  duration: "5m"
