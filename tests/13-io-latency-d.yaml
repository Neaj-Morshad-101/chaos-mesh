apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata:
  name: mssql-io-latency-partial
  namespace: chaos-mesh
spec:
  action: latency
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  volumePath: /var/pv
  path: /var/pv/data/**/*
  delay: "500ms"
  percent: 50
  duration: "5m"
  containerNames:
    - postgres
