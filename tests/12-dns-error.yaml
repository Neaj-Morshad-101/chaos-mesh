apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: mssql-primary-dns-error
  namespace: chaos-mesh
spec:
  action: error
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  duration: "2m"
