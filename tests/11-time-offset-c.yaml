apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: mssql-time-offset-all
  namespace: chaos-mesh
spec:
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
  timeOffset: "-1h"
  clockIds:
    - CLOCK_REALTIME
  duration: "3m"
