apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: mssql-memory-stress-primary
  namespace: chaos-mesh
spec:
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  stressors:
    memory:
      workers: 1
      size: "800MB"
  duration: "5m"
