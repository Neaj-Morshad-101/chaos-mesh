apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: mssql-kill-process-replica
  namespace: chaos-mesh
spec:
  action: container-kill
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "secondary"
  containerNames:
    - postgres
  duration: "30s"
