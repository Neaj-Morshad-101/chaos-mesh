apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata:
  name: mssql-io-attr-override-write-protected
  namespace: chaos-mesh
spec:
  action: attrOverride
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "sqlserver-ag-cluster"
      "kubedb.com/role": "primary"
  volumePath: /var/pv
  path: /var/pv/data/**/*
  attr:
    perm: 400
  percent: 100
  duration: "3m"
  containerNames:
    - postgres
